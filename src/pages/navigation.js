import React from 'react'
import { Navbar } from 'react-bootstrap'

const Navigation = () => {
    return (
        <Navbar bg="dark" variant="dark">
        <Navbar.Brand href="#home"> Change For Change </Navbar.Brand>
        </Navbar>
    );
}

export default Navigation;